import { Mode } from '../mode.js'

export class Movable extends Mode {

  init (options) {
    super.init(options)
  }

  destroy () {
    super.destroy()
  }

  load (term, options) {
    super.load(term, options)
  }

  unload (term) {
    super.unload(term)
  }

}
